# Stock image, no customization - this service directory exists so the
# renderer follows the repo's one-Dockerfile-per-service/pinned-upstream-tag
# convention instead of pinning a public tag inline in docker-compose.yml.
# Ships headless Chromium; that is where its ~1g memory limit goes.
FROM grafana/grafana-image-renderer:v5.12.0
