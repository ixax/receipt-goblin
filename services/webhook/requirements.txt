fastapi==0.115.0
uvicorn[standard]==0.30.6
clickhouse-connect==0.8.3
redis==5.0.8
PyYAML==6.0.2
prometheus-fastapi-instrumentator==7.0.0
# Rust-based, faster than stdlib json for the full ~360KB-1.5MB
# StandardLoggingPayload round-trips (common.ingest_parsing's _source_row) -
# see AGENTS.md "Why a queue in front of ClickHouse".
orjson==3.10.7
