fastapi==0.115.0
uvicorn[standard]==0.30.6
clickhouse-connect==0.8.3
redis==5.0.8
PyYAML==6.0.2
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.21.0
# Only used by the `loadtest` role (src/loadtest.py) to replay captured
# traffic concurrently against webhook - harmless to bake into the other
# roles, same as migrations/ being baked in for the `migrate` role.
aiohttp==3.10.10
